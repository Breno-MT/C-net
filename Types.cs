using System;

namespace Name
{
    class Types
    {
        static void Main(string[] args)
        {
            int IamNumber5 = 5;
            /* Comments are important */
            float pi = 3.1415;
            bool isBoolean = true;
            string myName = "Breno";
            // store only a single char
            char at = "@";

            /*
            byte -> 0 to 255
            signed byte -> -128 to 127
            unsigned byte -> 0 to 255
            short -> -32,768 to 32,767
            signed short -> -32,768 to 32,767
            unsigned short -> 0 to 65,535
            long -> too much bro
            signed long -> same bro
            unsigned long -> from 0 to too much bro
            float -> up to 7 digits e.g: 9.1234567F is a float
            double -> e.g: 0.42d is a double
            */

            // Nullable
            //string? myName = "";
            // int? number = null;

            // Reference Type
            //Reference type data types are string, class, Array etc...
            

            // Types of Comments
            // single line -> //
            //singleline yay
            
            // multiline -> /**/
            /*Hello, World!
            That's... well.
            */

            // xml -> ///
            ///<summary>
            //help
            ///</summary>
        }
    }
}